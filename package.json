{
  "name": "bizzy-base-transfer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check frontend/app.js",
    "serve:frontend": "python3 -m http.server 4173 --bind 127.0.0.1 --directory frontend"
  }
}
